defaults:
  - model: amsdf_lav
  - writer: wandb_inf
  - metrics: inference_full
  - datasets: fakeavcelebs_full
  - dataloader: dataloader
  - transforms: example
  - _self_
inferencer:
  device_tensors: ["av_audio", "av_video", "vivit_frames", "aasist_audio", "labels"] # which tensors should be on device (ex. GPU)
  device: auto # device name or "auto"
  save_path: "inference/fakeavcelebs" # any name here, can be a dataset name
  seed: 1
  from_pretrained: "/home/ialarin/deepfake_detection/saved/aasist_full/checkpoint-epoch10.pth" # path to the pretrained model
